{
  "ConnectionStrings": {
    "Db": "Server=localhost; Database= db_CrudOperationWithDto; Integrated Security= True; TrustServerCertificate=True;"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}

// appsetings file is used to store the configuration settings for the application, 
//such as database connection strings, logging levels, and other application-specific settings. In this example, it contains a connection string for a database named "db_CrudOperationWithDto" hosted on a local server, along with logging configurations and allowed hosts settings.
